---
layout: default
title: "light_source.get_style(index)"
parent: light_source
grand_parent: API Reference
nav_order: 6
---

# `light_source.get_style(index)`

Returns the animation string registered for a light style.

**Parameters**
- `index` (`number`) - the 1-based style index

**Returns**
- `string` - the style string, or `"m"` when not running on the server
