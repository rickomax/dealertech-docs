---
layout: default
title: ui_image
parent: API Reference
has_children: true
nav_order: 25
---

# `ui_image`

`ui_image` is a UI image element that renders a texture loaded from the engine resources.

`ui_image` inherits from [`ui_element`](ui_element.html): `set_position`, `set_selectable`, `destroy`, `center_on_x`, `rebuild`.
